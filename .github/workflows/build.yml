name: build and push docker image

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      registry:
        image: neurosnap/ptun:latest
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          USERNAME: erock
        ports:
          - 8443:8443
        options: >-
          --health-cmd "curl -f http://localhost:8443/v2/nvimsh/tags/list"
          --health-interval 30s
          --health-timeout 10s
          --health-retries 5
          --health-start-period 30s
    steps:
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        push: true
        tags: localhost:8443/nvimsh:latest
